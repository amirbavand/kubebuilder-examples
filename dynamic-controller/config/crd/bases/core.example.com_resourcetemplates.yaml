---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: resourcetemplates.core.example.com
spec:
  group: core.example.com
  names:
    kind: ResourceTemplate
    listKind: ResourceTemplateList
    plural: resourcetemplates
    singular: resourcetemplate
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ResourceTemplate is the Schema for the resourcetemplates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourceTemplateSpec defines the desired state of ResourceTemplate
            properties:
              resources:
                description: Foo is an example field of ResourceTemplate. Edit resourcetemplate_types.go
                  to remove/update
                items:
                  description: ResourceReference defines a reference to a Kubernetes
                    resource
                  properties:
                    group:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    version:
                      type: string
                  required:
                  - group
                  - kind
                  - name
                  - version
                  type: object
                type: array
            type: object
          status:
            description: ResourceTemplateStatus defines the observed state of ResourceTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
